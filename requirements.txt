# Pinned to the versions used for the published results (Python 3.13).
# Captured from the project virtual environment, which also carries the patched
# engine as onsset 2.1.dev24+gc154ece12 — not from a base conda install.

numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
PyYAML==6.0.3
openpyxl==3.1.5
matplotlib==3.11.0
numba==0.65.1

# Required by the vendored onsset package, not by the pinned stack above. These
# were implicit in the original conda environment and so were missing from this
# file; a from-scratch venv fails without them.
setuptools<81      # onsset/__init__.py imports pkg_resources, removed in setuptools 84
geojson            # imported by onsset.py; absent from OnSSET's own install_requires
requests           # ditto

# Geospatial stack — needed only by the spine-building stages (s01-s03)
rasterio==1.5.0
pyogrio==0.12.1
shapely==2.1.2
geopandas==1.1.3

# Dev dependency — not used by the pipeline itself, only to run `pytest test/ -v`
pytest==9.1.1

# OnSSET is NOT installed from PyPI. It is built from upstream commit c154ece with the
# five-change patch in patches/ applied. See patches/README.md and REPRODUCING.md.
