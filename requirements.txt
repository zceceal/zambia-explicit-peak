# Pinned to the versions used for the published results (Python 3.13).

numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
PyYAML==6.0.3
openpyxl==3.1.5
SALib==1.5.2
matplotlib==3.11.0
numba==0.65.1

# Geospatial stack — needed only by the spine-building stages (s01-s03)
rasterio==1.5.0
pyogrio==0.12.1
shapely==2.1.2
geopandas==1.1.3

# OnSSET is NOT installed from PyPI. It is built from upstream commit c154ece with the
# two-line patch in patches/ applied. See patches/README.md and REPRODUCING.md.
